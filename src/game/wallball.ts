t         — ball lands outside the court lines
//    • Body        — the ball strikes a player
//    • Out of turn — hitting when it isnt your turn
//
//  SHOTS real schoolyard names)
//    DRIVE        standing click        honest pace, safe
///  /  SCRAPIE       crouch + c─lic─k        ─ski──d─s─ i─n────────────── ─l─o─w─────────── ─a─n─d─ ───f─l─a─t─── o─ff t─he── w─a─l─l──
───
//    SM/A/SH  W A LL BA L   L  j— umreapl,  pcllaiyckgr foaundlli nrg ul  selsa
ms in short and rockets out
//
///  /   B ALBAY   YO   UT
   right click           die//s  r i g OhtN Ea watl l.t hOeNE b acousret  oinf th ef ronwta ofll
    it. BOTH players stand in /th/ e 
      CROSS-COURT/ /     cSAME arera faociuncg ht h+e  rwighatl lclick  fires it to the far corner
      // ─────────────────────────────────────────────────────────────

      imp. oThere rtis no divi ding* as  line and THREE fromn "
      three;
      //    isimdpeosr"t —  sfx }y foroum  s"a./re tahude isopac;e
      //  and take turns.
      // 
      // //
      // // ── Court geometry ─────/──/─  ─T──HE─── ─S─E────────Q─UE──NCE──── ───t─hi──s ─i─s────
      //  the whole gamee)x
      // port const WALL_Z       = 0;      // the wall face lives at z = 0
      // // ex por t  hcoinstt →  CbOUaRT_lDEPlT H  = 9.0; b  o u/n/ ccoeurs t rOunNs CEfr omo the wn the blacktoapl l →o buta ltlo  hi+tZs
      //  the WALL
      // export c/ons/t     C   O U→ RbTa_lHAl LrFeb_ounW ds=  →4 .b0ou; nces ONC E   o/n / half width otfh et heb lpaaciknttoped box
      // 
      // // e x p o rt  c o →n stt hWeA LL_OTHHEERIG HTp l = ay5.er2;    hi /ts/ i tc →l earre ptehaits
      //  and youre out
      // //
      // expor//t  c SoERnVsEt
      //  GROUND_Y     = 0.13;/
      // /    Server holds the ball aenxdp ostrtr iconkses ti t BALL_R down s      = 0.11;o 
      // it bounces once
      // 
      // //    and carries into the wall. Play is live f/ro/ ── Balml  phthyasit csmome n─t.───
      // ──────────────────────────/─/─
      // ─────────────
      // //  FOULS /o/pp Folneoantty s acnodr ges enae prouso:int )t
      // he ball ha/ng/s,  tr av  e• lVso ldleeye p  of f    —t hhei ttbirnicg bekfsor ea ntd
      // he ball bounces after // theis  eawasy tllo
      //  read. Every shot st/eers/ w i t  h • thDoe uble bounce — letting itm oubse.o
      // unce twice before you hit
      // export const GRAVITY       = 11.5;   // lower = higher, longer flight
      // "